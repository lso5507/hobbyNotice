<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hobby.mappers.board">


	<!--검색 기능 by 김정태-->
	<!-- 제목 검색 게시물 총 갯수 (0605 추가) -->
	<select id="searchCount" parameterType="String" resultType="int">
		select count(bdCno) from board
			WHERE bdTitle LIKE CONCAT ('%', #{keyword}, '%');
	</select>
	
	
	<!-- 게시물 검색 -->
	<select id="listPageSearch" parameterType="hashMap" resultType="com.daelim.hobby.Vo.VOBoard">
		select
			bdCno, bdTitle, bdName, bdDate, bdHit
		from board
	
		<if test='searchType.equals("검색항목 제목인 변수명")'>
			WHERE bdTitle LIKE concat('%', #{keyword}, '%')
		</if>
	
		<if test='searchType.equals("검색항목 내용인 변수명")'>
			WHERE bdContent LIKE concat('%', #{keyword}, '%')
		</if>
	
		<if test='searchType.equals("검색항목 제목+내용인 변수명")'>
			WHERE bdTitle LIKE concat('%', #{keyword}, '%')
			or bdContent LIKE concat('%', #{keyword}, '%')
		</if>
	
		<if test='searchType.equals("검색항목 작성자인 변수명")'>
			WHERE bdName LIKE concat('%', #{keyword}, '%')
		</if>
	
		order by bdCno desc
			limit #{displayPost}, #{postNum}
	</select>
	
</mapper>